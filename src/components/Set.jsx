import React from 'react'

const Set = () => {
  return (
    <>
      <div style={{marginTop: "30px"}}>
        <h1>Sets</h1>
        <p>Does not allow duplicate elements and has no guaranteed order (depending on the implementation). 
          Sets are commonly used when you need to ensure that each element is unique and don't care about the ordering. 
        </p>
      </div>

      
    </>
  )
}

export default Set